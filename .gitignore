# Python virtual environment (local only, not shipped)
.venv/

# FreeSurfer subjects_dir layout, materialized on demand by
# dev_tools/rearrange_fsaverage_into_subjects_dir.sh and
# dev_tools/rearrange_fs_LR_32_into_subjects_dir.sh (not shipped)
subjects_dir/
